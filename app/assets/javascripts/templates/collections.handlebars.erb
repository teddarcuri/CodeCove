<div class="flex">
	<ul class="trench">
	    {{#each}}
	    <li>
	        {{#link-to 'collection' this}}
	        <span class="name">{{name}}</span>
	        {{/link-to}}
	    </li>
	    {{/each}}
	</ul>
	<div class="container padded">
		{{outlet}}
	</div>
</div>
