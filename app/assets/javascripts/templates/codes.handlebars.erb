<div class="flex">
	<ul class="trench">
	    {{#each}}
	    <li>
	        {{#link-to 'code' this}}
	        <span class="name">{{name}}</span>
	        <span class="name">{{created_at}}</span>
	        {{/link-to}}
	    </li>
	    {{/each}}
	</ul>
	<div class="container codes">
		{{outlet}}
	</div>
</div>
